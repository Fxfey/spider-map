/**
 * Placeholder for step 3.1 — proves the React build is served by the plugin's
 * embedded server. The Leaflet claim map replaces this at 3.2.
 */
export default function App() {
  return (
    <main>
      <h1>spider-map</h1>
      <p>React app is served by the plugin.</p>
      <p className="hint">
        The claim map replaces this at <code>Milestone 3.2</code>.
      </p>
    </main>
  )
}
